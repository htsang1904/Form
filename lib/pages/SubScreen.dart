import 'package:flutter/material.dart';

class SubScreenPage extends StatelessWidget {
  const SubScreenPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        body: Container(),
      );
}
